{{- if eq .chezmoi.username "vantesh" }}
[user]
    name = "{{ .git.username }}"
    email = "{{ .git.email }}"
    signingkey = "{{ .git.signingKey }}"

[gpg]
    format = ssh

[commit]
    gpgsign = true

{{ else }}
[user]
    name = "{{ .git_username }}"
    email = "{{ .git_email }}"
{{ end }}
